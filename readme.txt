Component Setup

Message me if you need help:

FitPlayerInput and FitPalante go on a master object.
FitAnimator goes on the mesh. The mesh is a child of the master object. This will also contain the animation component and will need anims.